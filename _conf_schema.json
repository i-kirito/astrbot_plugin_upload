{
    "admin_only": {
        "description": "限制仅管理员可用",
        "type": "bool",
        "hint": "开启后只有 AstrBot 配置的管理员才能使用插件安装、卸载、更新等功能",
        "default": true
    },
    "astrbot_url": {
        "description": "AstrBot API 地址",
        "type": "string",
        "hint": "AstrBot 后台 API 地址。本地部署保持默认即可，Docker 部署可能需要改为容器内网地址",
        "default": "http://localhost:6185"
    },
    "api_username": {
        "description": "API 登录用户名",
        "type": "string",
        "hint": "AstrBot 后台的登录用户名，默认为 astrbot",
        "default": "astrbot"
    },
    "api_password": {
        "description": "API 登录密码",
        "type": "string",
        "hint": "AstrBot 后台的登录密码，用于调用插件安装接口",
        "default": "",
        "secret": true
    }
}
